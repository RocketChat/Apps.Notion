export enum CommandParam {
    CONNECT = "connect",
    DISCONNECT = "disconnect",
    CREATE = "create",
    HELP = "help",
    COMMENT = "comment",
    WORKSPACE = "workspace",
}

export enum SubCommandParam {
    DATABASE = "db",
}
